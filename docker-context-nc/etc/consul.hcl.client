datacenter = "dc1"
data_dir = "/opt/consul"

#
# bind_addr = "{{ GetInterfaceIP \"eth1\" }}"

client_addr = "0.0.0.0"

#
log_level = "INFO"

retry_join = [ "n_s1" ]

# Consul Connect
connect {
  enabled = true
}

ports {
  grpc = 8502
}
